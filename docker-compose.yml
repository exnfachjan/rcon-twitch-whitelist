services:
  twitch-whitelist-bot:
    image: exnfachjan/twitch-whitelist-bot:latest
    container_name: twitch-whitelist-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
